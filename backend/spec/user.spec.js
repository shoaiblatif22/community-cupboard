const UsersController = require('../controllers/user');


describe('User Registration', () => {
  test('should successfully register a new user with valid inputs', async () => {
    // Test data
    /*const newUser = {
      first_Name: 'John',
      last_Name: 'Foster',
      full_Address: '123 Main St, City, Country',
      email: 'john.foster@example.com',
      password: 'hashedPassword',
    };

    // Call the function being tested (e.g., registerUser)
    const result = await UsersController.Create({
      body: newUser,
    });

    // Assert the result is as expected
    expect(result.status).toBe(201);
    expect(result.body).toEqual({ message: 'OK' });
  });*/
});
});


